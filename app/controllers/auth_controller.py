class AuthController:
    def authenticate(self, domain, server, username, password):
        # Logique d'authentification (à implémenter plus tard)
        # Pour l'instant, on simule une connexion réussie
        return True